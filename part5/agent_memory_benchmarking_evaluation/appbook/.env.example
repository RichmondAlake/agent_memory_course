# Required — the only LLM provider this app uses.
ANTHROPIC_API_KEY=sk-ant-...

# Optional — Oracle AI Database (defaults match the local oracle-free container).
# DB_USER=VECTOR
# DB_PASSWORD=VectorPwd_2025
# DB_CONNECT_STRING=localhost:1521/FREEPDB1

# Optional — overrides.
# BENCH_AGENT_MODEL=claude-opus-4-8
# BENCH_SMALL_MODEL=claude-haiku-4-5
# BENCH_LIVE_TURNS=8
